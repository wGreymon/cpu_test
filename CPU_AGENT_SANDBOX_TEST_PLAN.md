# CPU 与 Agent 沙箱测试方案

> 版本：V1.2.3
> 适用对象：Linux x86_64 / ARM64 服务器
> 目标：**为 Agent 沙箱业务选出最合适的 CPU 平台**。其中平台基础性能测试（OS、CPU 计算、数据访问、持续性能与能效）用于解释各平台的能力与瓶颈来源；Agent 沙箱应用测试（性能、隔离、并发密度、稳定性）是选型结论的直接依据。

**V1.2 变更**：全文重组为两大部分——第一部分"平台基础性能测试"（按系统能力 / CPU 核心能力 / 内存能力三层组织），第二部分"Agent 沙箱应用测试"（保留框架，具体维度与用例待细化）。原 V1.1 中 Agent 与沙箱的详细用例草稿移至附录 A 保留。新增 core-to-core latency、lmbench、MLC loaded latency 等测试项，环境记录中新增安全缓解措施（mitigations）字段。

**V1.2.1 变更（2026-07-31）**：§5 新增"细化输入"小节，记录第二部分设计的三项关键输入（两种测试模式、能力全景清单、测量包裹层），供 V1.3 展开；配套 `PART1_TEST_SPEC.md` 升至 V0.2（对齐 Intel 首测实际执行）。

**V1.2.2 变更（2026-07-31）**：第一部分改为四类能力组织：OS 能力、CPU 计算能力、数据访问能力、持续性能与能效；修正 MLC、MEM-07、mitigations 和本轮 CPU-03 的测试口径，并与 Intel 冒烟后的脚本实现对齐。

**V1.2.3 变更（2026-07-31）**：补齐正式跨平台可比性约束：全 CPU governor 验证、GiB 级 L3 正确解析、STREAM 相同工作集、非连续 NUMA 节点支持；AMD 默认不依赖 Intel MLC；结果记录补充 NUMA 与框架版本字段。

## 1. 总体结构

Agent 通常由大模型负责推理和决策，再调用 Python、Shell、编译器、数据库或浏览器等工具。Agent 沙箱是这些工具的受限执行环境，用于隔离文件、进程、网络和权限，并限制 CPU、内存、运行时间等资源。

```text
大模型服务（可运行于 CPU、GPU 或其他加速器）
                    │ 生成工具调用
                    ▼
              Agent 调度程序
                    │ 创建任务
                    ▼
   沙箱（进程 / 容器 / gVisor / microVM）
                    │
                    ▼
 Python / Shell / 编译 / 检索 / 数据处理 / 浏览器
```

本方案分为两大部分：

| 部分 | 内容 | 作用 |
|---|---|---|
| **第一部分：平台基础性能测试** | OS、CPU 计算、数据访问、持续性能与能效四类基准 | 解释平台硬件能力与瓶颈来源，提供可跨平台对比的基线 |
| **第二部分：Agent 沙箱应用测试** | 真实 Agent 任务、沙箱生命周期、隔离与并发容量（框架待细化） | 给出最终业务表现，是选型结论的直接依据 |

两部分的关系：第一部分回答"这个平台的硬件能力如何、为什么"，第二部分回答"跑 Agent 沙箱业务谁更好"。通用基准解释原因，真实负载决定结论。

### 1.1 测试对象

- 待比较的 CPU 或服务器平台。
- 裸机环境，作为性能基线。
- 至少一种轻量沙箱，例如 Docker 或 Podman。
- 可选的强隔离沙箱，例如 gVisor、Kata Containers 或 Firecracker。
- 固定版本的 Agent 任务集。

### 1.2 不在首轮范围内的内容

- Agent 推理质量、任务规划质量和模型正确率。
- GPU/NPU 本身的算力测试。
- 未经授权的宿主机攻击或真实逃逸测试。

---

# 第一部分：平台基础性能测试

平台基础性能按四类组织。测试对象不只是 CPU 核心本身，还包括操作系统交互路径、内存/存储数据路径以及长时间运行状态；分开测量才能定位瓶颈。

| 类别 | 测什么 | 与 Agent 场景的关系 |
|---|---|---|
| OS 能力 | CPU + 内核 + 调度 + 系统调用的整体表现 | Agent 沙箱大量进行进程创建、系统调用和上下文切换 |
| CPU 计算能力 | 标量/通用计算、多核扩展、核间通信 | 决定单个工具任务的执行速度和整机吞吐上限 |
| 数据访问能力 | 缓存、内存带宽/延迟、NUMA；存储/文件系统辅助项 | Python、编译、检索经常受数据访问限制 |
| 持续性能与能效 | 长时吞吐、频率、温度、整机功耗 | 决定长期服务能力、运行成本和是否发生降频 |

## 2. OS 能力测试

**为什么测**：这一层测的不是纯硬件，而是"操作系统跑在这颗 CPU 上的整体表现"。负载穿过内核，覆盖调度、系统调用、进程管理等路径。它是 Agent 沙箱的重要解释维度；最终权重应在第二部分业务负载确定后再设定。

| ID | 测试项 | 目的 | 推荐工具 | 主要结果 |
|---|---|---|---|---|
| OS-01 | 系统综合基准 | 快速横向对比整机系统表现 | UnixBench | 综合分、各子项分数 |
| OS-02 | 系统调用延迟 | 沙箱敏感路径（seccomp、gVisor 拦截均放大此项） | lmbench、perf bench syscall | ns/op |
| OS-03 | 进程创建 | 短任务与沙箱启动能力 | lmbench lat_proc、定制脚本 | µs/次、P95 |
| OS-04 | 上下文切换 | 高并发调度开销 | lmbench lat_ctx、perf bench sched | 延迟、ops/s |
| OS-05 | 调度器压力 | 大量任务下调度器的吞吐与公平性 | hackbench | 耗时、抖动 |

说明：

- UnixBench 负载较老，跨 x86/ARM 对比时以 lmbench + 各单项结果为准，UnixBench 综合分仅作参考。
- 测试时必须记录内核安全缓解措施状态（见 §8）。同厂商平台策略应一致；Intel 与 AMD 的漏洞集合不同，采用各自生产默认策略，记录差异并解释其对系统调用结果的影响。

## 3. CPU 计算能力测试

**为什么测**：处理器硬件本身的能力，决定单个工具任务的执行速度（单核）与并发任务的总吞吐（多核）。尽量绕开操作系统影响。

| ID | 测试项 | 目的 | 推荐工具 | 主要结果 |
|---|---|---|---|---|
| CPU-01 | 单线程标量计算 | 单个短任务、串行任务的响应速度 | sysbench、7-Zip 内置基准 | events/s、耗时 |
| CPU-02 | 多线程通用计算与扩展性 | 并发 Agent 或批量任务的处理能力 | sysbench、7-Zip | 吞吐、并行扩展效率 |
| CPU-03 | 浮点/SIMD | 科学计算与向量指令能力（AVX2/AVX-512/AMX、NEON/SVE） | HPL、CoreMark-Pro 或同类 | GFLOPS、耗时 |
| CPU-04 | 压缩/解压 | 模拟常见工具任务 | zstd、7-Zip | MB/s |
| CPU-05 | 加密/哈希 | 模拟签名和内容校验 | OpenSSL speed | ops/s、MB/s |
| CPU-06 | 核间通信延迟 | 揭示 CPU 内部拓扑（CCX/CCD/跨 socket），解释多核任务迁移与锁竞争开销 | core-to-core-latency | N×N 延迟矩阵（ns） |

多线程测试至少覆盖 1、物理核数的 25%、50%、100%，以及启用全部逻辑核。计算扩展效率：

```text
并行扩展效率 = N 线程吞吐 / (单线程吞吐 × N)
```

说明：

- 不再设置额外的综合计算跑分项：CPU-01～06 已覆盖本项目需要的单核、多核、压缩、加密和核间通信能力，后续使用真实 Agent 任务回放验证实际收益。
- CPU-03 与当前 Coding/Terminal Agent 的相关性较低，本轮 Intel/AMD 对比暂不执行；如后续任务包含本地数值计算，再选定 DGEMM/HPL 等正式工具补测。
- 跨架构对比必须使用同一工具版本及该工具约定的固定编译参数，并分别使用架构原生二进制；禁止只在一侧启用 `-march=native`、PGO 等额外优化。
- 若平台含异构核（P 核/E 核、big.LITTLE），CPU-01/02 需分别测试 P 核绑定、E 核绑定和默认调度三种情况，并记录核类型拓扑。

## 4. 数据访问能力测试

### 4.1 内存与 NUMA

**为什么测**：内存控制器集成在 CPU 内，带宽、延迟和 NUMA 特性是平台能力的组成部分。Python、编译器、浏览器和代码检索任务经常受内存与缓存限制。

| ID | 测试项 | 目的 | 推荐工具 | 主要结果 |
|---|---|---|---|---|
| MEM-00 | 内存正确性检查 | 测前排除硬件故障（非性能测试） | memtester | pass/fail |
| MEM-01 | 顺序读写带宽 | 可持续内存带宽（单线程 = 单核上限） | STREAM（重点看 Triad） | GB/s |
| MEM-02 | 缓存/内存延迟阶梯 | L1/L2/L3/内存各层级访问延迟 | lmbench lat_mem_rd、Intel MLC | ns（随工作集变化的阶梯曲线） |
| MEM-03 | NUMA 延迟矩阵 | 每个节点访问每个节点内存的延迟 | Intel MLC、numactl + 内存基准 | ns 矩阵、远端/本地比例 |
| MEM-04 | NUMA 带宽矩阵与峰值带宽 | 不同读写比例下的最大带宽 | Intel MLC、STREAM + numactl | GB/s 矩阵 |
| MEM-05 | 负载下延迟（loaded latency） | 带宽压力增大时延迟的恶化曲线，直接决定高并发下的实际表现 | Intel MLC | 带宽—延迟曲线 |
| MEM-06 | 多线程带宽扩展 | 多核同时访存的带宽竞争 | STREAM 多线程（OpenMP） | GB/s、扩展曲线 |
| MEM-07 | 随机访问 | 大工作集随机访问（解释器、图结构） | 固定种子 pointer_chase；tinymembench/sysbench 仅作参考 | ns、ops/s |

说明：

- Intel MLC 为闭源工具，官方面向 Intel 平台；AMD 上只能作为兼容性验证后的补充数据。跨 Intel/AMD/ARM 的共同基线是 STREAM + lmbench + numactl。
- STREAM 编译参数影响显著：使用 `-O3`，每个数组至少为候选平台最大整机 L3 的 4 倍；正式对比时所有平台通过 `STREAM_ARRAY_MIB` 使用相同容量。
- 多路平台必测 MEM-03/04（本地 vs 跨 NUMA）；单路平台可跳过。

### 4.2 存储与文件系统（辅助项）

| ID | 测试项 | 目的 | 推荐工具 | 主要结果 |
|---|---|---|---|---|
| OS-06 | 4K 随机读、大块顺序读 | 解释依赖加载、仓库读取和日志差异 | fio | IOPS、MB/s、P95/P99 |
| 可选 | 小文件与元数据 | 解释 `find/rg/git` 等真实任务差异 | fs_mark 或固定仓库回放 | files/s、任务耗时 |

OS-06 沿用历史编号，但归入数据访问能力。存储硬件不一致时，本组只用于解释 Agent 任务差异，不进入纯 CPU 排名。

## 5. 持续性能与能效测试

**为什么测**：短时跑分容易受睿频影响，不能说明服务器在持续并发下的吞吐、温度和运行成本。

| ID | 测试项 | 目的 | 推荐工具 | 主要结果 |
|---|---|---|---|---|
| CPU-08 | 持续性能与降频 | 观察长时间满载后的性能保持能力 | stress-ng 造压；sysbench 压力前/后复测；turbostat 监控 | 前后吞吐差、频率、温度、bogo-ops/s |
| CPU-09 | 能耗与能效 | 计算相同吞吐所需整机功耗 | 外置功率计/PDU，或 ipmitool DCMI；RAPL 辅助 | 平均/峰值 W、task/s/W、J/task |

stress-ng 的 bogo-ops 只用于确认同一平台的压力负载是否稳定，不作为不同 CPU 的核心计算排名。能效对比统一使用整机功耗；RAPL 只覆盖 CPU 封装，不能与整机功耗混比。

---

# 第二部分：Agent 沙箱应用测试（框架，待细化）

> 本部分当前为框架性内容。Agent 沙箱应从哪些维度评估、各维度的具体用例和指标门槛，待进一步调研后在下一版本中细化。V1.1 已起草的详细用例（Agent 任务集、沙箱生命周期、隔离与资源控制、并发容量、稳定性）完整保留在附录 A，供细化时参考。

## 6. 目标与初步维度

本部分需要最终回答：不同 CPU 平台运行 Agent 沙箱业务时，谁的性能、隔离能力、稳定性更好？

初步考虑的评估维度（待确认）：

1. **真实 Agent 任务性能**：Python 启动、代码检索、编译、单元测试等固定任务集在各平台的完成时间与吞吐。
2. **沙箱生命周期与开销**：冷/热启动、销毁时间；沙箱内运行任务相对裸机的性能损耗。
3. **资源限制与安全隔离**：CPU、内存、进程数、时间限制是否生效；文件、进程、网络、权限隔离是否生效。
4. **并发密度与容量**：单机最大稳定并发沙箱数及其尾延迟表现。
5. **长时间稳定性**：持续负载与反复创建销毁下的资源回收、泄漏和失败率。

第一部分的微基准结果（尤其 OS 层）应能解释本部分的差异来源。

### 6.1 细化输入（2026-07-31 记录，供 V1.3 展开）

1. **两种测试模式，分工明确**：
   - **固定动作回放**：各平台执行完全相同的命令序列（任务集固定版本、固定输入），保证严格可比——作为平台排名依据；
   - **完整 Agent 任务**：让 Codex / Claude Code 等真实 Agent 自主完成任务，验证真实业务表现——作为验证与演示，不直接参与排名（模型行为的随机性会破坏可比性）。
2. **能力全景 → 代表任务**（每类只选少量、固定、可自动验证的代表任务，避免测试矩阵膨胀）：Agent 编排（JSON 解析/状态管理/任务调度）、代码检索（rg/find/git）、Shell 短命令、Python 启动与导包、代码修改（读取/替换/生成补丁）、编译构建、单元测试、依赖安装（本地缓存）、数据处理（SQLite/压缩/图像）、沙箱管理（创建/执行/超时/销毁）、多 Agent 并发、长时运行。其中"Agent 编排"与"代码修改"为附录 A 任务集未覆盖的新增能力。浏览器（Playwright + 本地固定网页）与本地向量检索（FAISS 固定索引）仅在沙箱实际配置这些工具时列为可选组；由远端服务提供的能力（模型 API、远程数据库）不计入本机性能。
3. **测量包裹层**：真实命令统一用外壳采集统计——重复/预热（复用 `scripts/lib/common.sh` 框架，或 hyperfine）、`/usr/bin/time -v`（CPU time、峰值 RSS——**RSS 直接决定单机并发密度上限**）、`perf stat`（IPC、cache miss、上下文切换，用于把 Agent 任务差异回溯到第一部分的微基准指标）。

---

# 共通部分

## 7. 测试原则

### 7.1 公平性

- 使用相同的操作系统大版本、内核策略、编译器和依赖版本。
- 使用相同的数据集、任务参数、沙箱镜像和超时配置。
- 明确记录物理核、逻辑核、NUMA、SMT、睿频和功耗模式。
- 容量不同的平台，**整机吞吐类**指标同时报告"整机结果"和"每核结果"；延迟、单线程、内存带宽类不做每核归一化。不能只比较总分。
- 跨架构测试时分别使用架构原生二进制，统一编译优化等级，禁止用仿真结果冒充原生结果。

### 7.2 可复现性

- 常规微基准预热至少 2 次、正式运行至少 5 次；STREAM（工具内部 20 轮）、MEM-00、CPU-06、CPU-08、UnixBench 等长时或套件型用例按执行规范使用明确例外次数。
- 延迟类结果在工具内部产生大量样本时报告 P50、P95、P99；外层少量重复只报告中位数、极差与 CV（5 个样本不足以支撑百分位）。
- 计算类结果变异系数（CV）以 3%、系统/Agent 类以 5% 作为稳定性参考线；超出时标记 `UNSTABLE`，检查干扰并进行一次受控补测。若在干净环境中仍复现，应将波动作为平台特征报告，而不是视为命令或硬件正确性失败。
- 保存原始日志、命令、配置、软件版本和时间戳。

### 7.3 安全性

- 资源耗尽和隔离测试只能在专用测试机或已验证的外层虚拟机内执行。
- 所有压力任务必须设置明确的 CPU、内存、进程数和时间上限。
- 不执行无边界 fork、磁盘写满、真实破坏或沙箱逃逸代码。

## 8. 环境与基线记录

### 8.1 硬件信息

每台测试机至少记录：

| 类别 | 必填字段 |
|---|---|
| CPU | 厂商、型号、架构、插槽数、物理核、逻辑核、基础/最大频率、TDP |
| 核拓扑 | 是否异构核（P/E 核）、各类核数量 |
| 指令集 | SSE/AVX2/AVX-512/AMX、NEON/SVE 等 |
| 缓存 | L1、L2、L3 容量及拓扑 |
| 内存 | 总容量、通道数、频率、NUMA 节点数 |
| 存储 | 型号、介质、接口、文件系统 |
| 网络 | 网卡型号、速率（网络测试启用时） |
| 固件 | BIOS/UEFI、微码版本、虚拟化开关 |
| 能耗 | 功耗读取方式、空闲功耗、环境温度 |

推荐采集工具：`lscpu`、`lstopo`、`numactl --hardware`、`dmidecode`、`lsblk`、`uname`。

### 8.2 软件信息

- Linux 发行版和内核版本。
- **安全缓解措施状态**：`lscpu` 的 Vulnerabilities 字段、内核 mitigation 启动参数。同厂商平台必须保持一致策略；跨厂商（Intel vs AMD）漏洞集合天然不同，不要求逐项一致——各平台采用生产默认安全策略，完整记录并在报告中解释差异。
- CPU governor、EPP、睿频、SMT、透明大页、NUMA 策略、`perf_event_paranoid` 和 `nmi_watchdog`。
- GCC/Clang、Python、Java、Rust 等运行时版本。
- Docker/Podman、containerd、runc、gVisor/Kata/Firecracker 版本。
- 沙箱镜像摘要（digest），不能只记录易变化的标签。
- 已启用的安全配置：namespace、cgroup v2、seccomp、AppArmor/SELinux。

### 8.3 测试前检查

- 机器无其他高负载任务，CPU 空闲率满足测试要求。
- 运行 MEM-00（memtester）排除内存硬件故障。
- 时间同步正常，磁盘空间和日志空间充足。
- 固定电源与性能策略；若测试节能模式，应作为独立实验组。
- 记录测试前 CPU 温度和空闲功耗。
- 清楚标记冷缓存测试与热缓存测试，不随意清除宿主机缓存。

## 9. 指标体系

| 维度 | 指标 | 单位或表达方式 |
|---|---|---|
| 延迟 | 单任务耗时、启动耗时、P50/P95/P99 | ms、s |
| 吞吐 | 单位时间完成任务数 | task/s、job/min |
| CPU | 利用率、CPU time、频率、IPC | %、s、GHz、instructions/cycle |
| 缓存 | miss rate、LLC 命中率 | % |
| 内存 | 带宽、延迟、峰值 RSS | GB/s、ns、MiB |
| 调度 | 上下文切换、迁核、run queue | 次/s、数量 |
| 存储 | IOPS、带宽、延迟 | IOPS、MB/s、ms |
| 稳定性 | 成功率、超时率、性能波动 | %、CV |
| 能效 | 每瓦吞吐、单任务能耗 | task/s/W、J/task |
| 成本 | 单任务成本、单位价格吞吐 | 元/task、task/s/万元 |

## 10. 数据记录格式

建议每次运行输出一条结构化记录，CSV 或 JSON 均可。至少包含：

第一阶段脚本已写入这些字段；尚未用统一测量外壳采集的 `cpu_time_ms`、`peak_rss_mb` 和逐任务 `energy_j` 暂记为 `null`，不得将空值解释为 0。功耗原始序列单独保存在 CPU-09 CSV 中，待后处理关联。

| 字段 | 示例 |
|---|---|
| run_id | 20260730-platformA-bare-001 |
| timestamp | ISO 8601 时间 |
| platform_id | cpu-a |
| cpu_model | 具体型号 |
| os_kernel | 发行版与内核 |
| mitigations | on/off/具体参数 |
| sandbox_type | bare/docker/gvisor/kata/firecracker |
| test_id | CPU-02 |
| concurrency | 16 |
| numa_policy | default/bind/interleave |
| iteration | 1～5 |
| duration_ms | 结果 |
| cpu_time_ms | 结果 |
| peak_rss_mb | 结果 |
| energy_j | 结果或空值 |
| exit_code | 0/非 0 |
| metric_valid | true/false/null（raw-only） |
| correctness | pass/fail/incomplete |
| git_commit | 测试框架版本 |
| framework_sha256 | 无 `.git` 的同步副本也可用的框架内容摘要 |
| notes | 异常说明 |

推荐目录结构：

```text
cpu_test/
├── configs/             # 平台、任务和沙箱配置
├── workloads/           # 固定任务实现
├── scripts/             # 环境采集、运行和汇总脚本
├── results/
│   ├── raw/             # 不修改的原始结果
│   └── processed/       # 清洗和汇总结果
├── reports/             # 图表与最终报告
└── CPU_AGENT_SANDBOX_TEST_PLAN.md
```

## 11. 实施计划

| 阶段 | 工作内容 | 产出 |
|---|---|---|
| 第 1 周 | 环境调查、指标确认、工具准备 | 环境清单、测试配置 |
| 第 2 周 | 第一部分：OS / CPU 计算 / 数据访问 / 持续性能与能效四类测试 | 平台基础性能基线数据与瓶颈分析 |
| 第 3 周 | Agent 沙箱评估维度调研与方案细化 | 第二部分正式测试方案（V1.3） |
| 第 4 周起 | 第二部分测试执行 | 沙箱开销、隔离检查、容量与稳定性数据 |

## 12. 结果分析与展示

最终报告至少包含：

1. **平台信息表**：硬件、系统和沙箱版本。
2. **系统能力对比**：系统调用、进程创建、上下文切换延迟。
3. **单核与多核图**：吞吐和并行扩展效率。
4. **核间延迟矩阵图**：拓扑热力图。
5. **内存与 NUMA 图**：延迟阶梯、本地/远端带宽、loaded latency 曲线。
6. **持续性能与能效图**：频率/吞吐随时间变化、task/s/W。
7. **（待第二部分细化后补充）** Agent 任务、沙箱开销、并发曲线与隔离检查表。

不建议只生成一个不透明的综合分。若确需排序，应先保留各维度原始结果，再根据项目目标设置权重，且权重应在看到结果之前确定。

---

# 附录 A：Agent 沙箱测试详细草稿（V1.1 原文保留，待评审细化）

> 以下内容为 V1.1 起草的 Agent 沙箱测试用例，尚未评审定稿，保留于此供第二部分细化时参考。

## A.1 Agent 真实任务集

所有输入数据应固定版本并计算校验和；任务必须有可自动验证的正确输出。

| ID | 任务 | 工作负载特征 | 建议输出指标 |
|---|---|---|---|
| AGT-01 | Python 启动、导入依赖并处理 JSON | 短进程、解释器、内存 | 启动时间、总耗时、RSS |
| AGT-02 | 对固定代码仓库执行文本检索 | 文件读取、正则、缓存 | files/s、P95 |
| AGT-03 | 编译固定 C/C++ 或 Rust 项目 | 多核、进程、I/O | 编译时间、CPU time |
| AGT-04 | 运行固定单元测试集 | 进程、解释器、混合负载 | tests/s、成功率 |
| AGT-05 | zstd 压缩与解压固定数据集 | 整数、内存 | MB/s、压缩率 |
| AGT-06 | SQLite 导入与查询 | 单核、存储、锁 | queries/s、P95 |
| AGT-07 | 图像缩放或格式转换 | SIMD、内存 | images/s、P95 |
| AGT-08 | 安装本地依赖包 | 小文件、解压、脚本 | 总耗时、峰值 RSS |
| AGT-09 | 多步骤复合任务 | 启动、检索、生成、测试 | 端到端耗时、成功率 |

推荐将 AGT-01、02、03、04、09 作为首轮必测集。

## A.2 沙箱生命周期与性能

| ID | 测试项 | 方法 | 主要结果 |
|---|---|---|---|
| SBX-01 | 冷启动 | 镜像已下载、实例未创建的状态 | P50/P95/P99 |
| SBX-02 | 热启动 | 使用预热镜像或实例池 | P50/P95/P99 |
| SBX-03 | 空任务执行 | 执行最小命令后退出 | 固定开销 |
| SBX-04 | 销毁与回收 | 检查进程、cgroup、挂载点残留 | 时间、残留数 |
| SBX-05 | 计算开销 | 在裸机和沙箱运行 CPU 微基准 | 开销百分比 |
| SBX-06 | 内存开销 | 在裸机和沙箱运行 MEM 测试 | 带宽损失、额外 RSS |
| SBX-07 | 文件系统开销 | 运行 fio 小文件与随机读写 | IOPS/带宽损失 |
| SBX-08 | Agent 开销 | 运行 AGT 必测集 | 延迟、吞吐损失 |
| SBX-09 | 并发启动 | 同时创建 N 个沙箱 | 总吞吐、P99、失败率 |
| SBX-10 | 混合并发 | 不同任务随机组合 | 吞吐、尾延迟、公平性 |

性能开销计算：

```text
耗时开销(%) = (沙箱耗时 - 裸机耗时) / 裸机耗时 × 100%
吞吐损失(%) = (裸机吞吐 - 沙箱吞吐) / 裸机吞吐 × 100%
```

## A.3 沙箱隔离与资源控制

隔离测试以"限制是否生效、宿主机是否稳定、任务是否正确终止"为判定重点，不执行真实破坏行为。

| ID | 测试项 | 预期结果 |
|---|---|---|
| ISO-01 | CPU 配额 | 平均 CPU 使用量接近限额，其他任务不被饿死 |
| ISO-02 | 内存上限 | 超限任务被沙箱终止，宿主机不发生 OOM |
| ISO-03 | 进程数上限 | 达到 PIDs 限制后创建失败，不影响宿主机 |
| ISO-04 | 执行超时 | 任务按时终止，无子进程残留 |
| ISO-05 | 文件边界 | 无法读取或修改未挂载的宿主机路径 |
| ISO-06 | 只读挂载 | 写操作被拒绝 |
| ISO-07 | 网络禁用 | 无法访问外部网络和宿主机敏感端口 |
| ISO-08 | 网络白名单 | 只能访问明确允许的目标 |
| ISO-09 | 权限限制 | 非 root、无额外 capability，受限系统调用被拒绝 |
| ISO-10 | 回收能力 | 异常退出后 cgroup、进程、挂载和临时文件被清理 |
| ISO-11 | 相邻租户干扰 | 一个压力任务不能突破配额拖垮其他沙箱 |

ISO-01～10 均属于功能性必过项。任何边界失效都不能用较高性能抵消。

## A.4 并发与容量测试方法

1. 从并发 1 开始，按 2 倍逐级增加并发。
2. 每一级持续运行固定时间和固定任务比例。
3. 记录吞吐、P95/P99、成功率、CPU、内存、run queue 和 I/O wait。
4. 当出现以下任一情况时，认为已越过稳定容量点：
   - 成功率低于 99%。
   - P95 延迟超过单并发基线的 2 倍（可按业务 SLO 调整）。
   - 发生不可接受的超时、OOM 或资源回收失败。
   - 吞吐不再增长，且尾延迟持续恶化。
5. 上一个满足全部目标的并发级别定义为"最大稳定并发数"。

## A.5 稳定性与热性能

| ID | 测试项 | 时长 | 观察内容 |
|---|---|---:|---|
| STB-01 | 满核持续计算 | 1 小时 | 频率、温度、吞吐、降频 |
| STB-02 | 70% 最大稳定并发 | 8 小时 | 成功率、P99、内存增长 |
| STB-03 | 沙箱反复创建销毁 | 1 万次或 2 小时 | 残留、失败率、泄漏 |
| STB-04 | 混合 Agent 任务 | 可选 24 小时 | 抖动、异常、资源回收 |

## A.6 测试矩阵（草稿）

| 变量 | 建议取值 |
|---|---|
| 执行环境 | 裸机、容器；可选 gVisor/Kata/microVM |
| 并发数 | 1、2、4、8、16、32，继续增加至过载点 |
| CPU 配额 | 0.5、1、2、4 核；不限额基线 |
| 内存配额 | 256 MiB、512 MiB、1 GiB、2 GiB |
| 任务类型 | 计算、内存、进程、文件 I/O、混合型 |
| 持续时间 | 短任务、10 分钟、1 小时；可选 8/24 小时 |
| NUMA 策略 | 默认、绑核绑内存、跨 NUMA（多路平台必测） |

## A.7 CPU 大模型推理测试（可选）

若目标平台还需要直接运行模型，应将推理测试作为独立结果展示，不能与沙箱工具任务合成一个总分。

- 固定模型文件、量化方式、推理框架和提交版本。
- 选择能在所有候选平台内存中运行的相同模型。
- 固定输入长度、输出长度、batch size 和线程数。
- 记录模型加载时间、TTFT、tokens/s、P95 延迟、峰值内存、功耗和每 Token 能耗。
- 对支持的指令集优化（AVX2/AVX-512/AMX、NEON/SVE）分别记录。

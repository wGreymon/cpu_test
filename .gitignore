# 工具与源码包（远程机器上由 01_setup_tools.sh 生成，不入库）
tools/

# 大文件测试数据（远程下载，只入库校验和）
workloads/silesia.tar
workloads/silesia.zip
workloads/*.bin

# fio 测试文件
*.fio.tmp
fio-test-*

# 测试原始结果：由每次运行自动生成，不入库
# 汇总后的 results/processed/ 与 reports/ 保留为可选的版本化成果
results/raw/

# 杂项
.DS_Store
__pycache__/
